apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pipeline-buffer-data-claim
  namespace: argo
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-client
  resources:
    requests:
      storage: 1Gi
