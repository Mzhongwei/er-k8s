apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-pipeline-buffer-data-claim
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  persistentVolumeReclaimPolicy: Delete
  hostPath:
    path: /var/lib/eaer/pv/pipeline-buffer-data-claim
  claimRef:
    namespace: argo
    name: pipeline-buffer-data-claim
  volumeMode: Filesystem
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
            - server2-labo
