apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-pipeline-kafka-data-claim
spec:
  capacity:
    storage: 20Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  persistentVolumeReclaimPolicy: Delete
  hostPath:
    path: /var/lib/eaer/pv/pipeline-kafka-data-claim
  claimRef:
    namespace: argo
    name: pipeline-kafka-data-claim
  volumeMode: Filesystem
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
            - domolandes
