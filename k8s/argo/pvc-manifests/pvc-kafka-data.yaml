apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pipeline-kafka-data-claim
  namespace: argo
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 20Gi
