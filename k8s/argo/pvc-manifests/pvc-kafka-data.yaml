apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pipeline-kafka-data-claim
  namespace: argo
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-client
  resources:
    requests:
      storage: 20Gi
