apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pipeline-reports-claim
  namespace: argo
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 50Mi
