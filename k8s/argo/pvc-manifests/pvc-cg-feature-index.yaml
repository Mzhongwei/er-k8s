apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pipeline-feature-index-cache-claim
  namespace: argo
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi