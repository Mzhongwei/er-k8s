apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pipeline-decision-evaluation-cache-claim
  namespace: argo
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-client
  resources:
    requests:
      storage: 100Mi