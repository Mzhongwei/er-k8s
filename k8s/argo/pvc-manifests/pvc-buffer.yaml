apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pipeline-buffer-data-claim
  namespace: argo
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 1Gi
