import socket
import time

LISTEN_HOST = "0.0.0.0"
LISTEN_PORT = 8080

# INCREMENTAL MODE
RANDOM_WALK_HOST = "service-random-walk"
RANDOM_WALK_PORT = 80

while True:
    print("Running...")
    time.sleep(1)