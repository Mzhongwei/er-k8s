FROM alpine:latest

# incremental_init.sh (the only thing this image runs) is mounted in at runtime via the
# eaer-incremental-init ConfigMap, not baked in here -- nothing else needs to be copied.
WORKDIR /app